
       for(char item : s.toCharArray()){
        if(item == "("){
            stack.push(')');
        }
       }
        else if(item == '{'){
            stack.push('}');
        }
        else if(item == '['){
            stack.push(']');
        }


       for(char i : s.toCharArray()){
        if(stack.top == i){
        }
       }
        Deque<String> stack = new ArrayDeque<>();
            stack.pop();
        }
        else{
            return 
        }
    }
}
